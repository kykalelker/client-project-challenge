<h3><%= @user.username %>'s Favorites List</h3>
<% if @movies == [] %>
<p>
	<%= @user.username %> hasn't added any movies to his favorites list.
</p>
<% else %>
<div>
	<% @movies.each do |movie| %>
	<div>
	<h4><%= link_to movie.title, movie_path(movie) %></h4>
		<%= image_tag movie.boxart_url,  :size => "240x320" %><br>
	</div>
	<% end %>
</div>
<% end %>
<strong><%= link_to 'Profile Page', user_path(@user) %></strong>
